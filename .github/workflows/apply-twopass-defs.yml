name: Apply two-pass top-level defs

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/apply-twopass-defs.yml'
      - 'evidence/semantic-ir-recovery/apply_twopass_defs.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch
        id: patch
        run: |
          set -euo pipefail
          python3 evidence/semantic-ir-recovery/apply_twopass_defs.py
          if git diff --quiet src/c_backend.rs; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Commit
        if: steps.patch.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/c_backend.rs
          git commit -m "feat(c-backend): two-pass top-level defs for mutual recursion (COMPILER-04/12)"
          git push origin HEAD:master
